import React from 'react';

const ChannelChatUI = ({ onUserSelect }) => {
    return (
        <div>
            <h2>Channel Chat</h2>
            {/* Channel chat UI elements go here */}
        </div>
    );
};

export default ChannelChatUI;
